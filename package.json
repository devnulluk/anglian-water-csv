{
  "name": "anglian-water-csv",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check static/app.js && node --check static/data.js && node --check static/api.js && node --check static/archive.js"
  },
  "engines": {
    "node": ">=22"
  }
}
